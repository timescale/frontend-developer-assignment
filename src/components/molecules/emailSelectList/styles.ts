import styled from "@emotion/styled";
import { Button } from "@chakra-ui/react";

export const StyledAccordionItem = styled(Button)`
  font-weight: normal;
  padding: 1rem;
  display: flex;
  justify-content: flex-start;
  border-radius: 0;
`;
